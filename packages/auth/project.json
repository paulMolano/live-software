{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "auth",
  "projectType": "library",
  "root": "packages/auth",
  "sourceRoot": "packages/auth/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint \"src/**/*.{ts,tsx,js,jsx}\"",
        "cwd": "packages/auth"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p packages/auth/tsconfig.json --noEmit"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/dist/packages/auth"
      ],
      "options": {
        "command": "tsc -p packages/auth/tsconfig.json"
      }
    }
  },
  "tags": [
    "scope:shared",
    "type:auth"
  ]
}
