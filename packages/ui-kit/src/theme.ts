import { createTheme } from '@mantine/core';

export const liveSoftwareTheme = createTheme({
	primaryColor: 'teal',
	fontFamily: '"Public Sans", "Segoe UI", sans-serif',
	headings: {
		fontFamily: '"Sora", "Public Sans", "Segoe UI", sans-serif',
	},
	defaultRadius: 'md',
});
